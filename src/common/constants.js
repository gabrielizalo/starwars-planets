import { createTheme } from '@mui/system';

const starWarsTheme = createTheme({
  palette: {
    background: {
      paper: '#C8CACF'
    },
    text: {
      primary: '#C09A43',
      secondary: '#545E6B'
    }
  }
});

export default starWarsTheme;
